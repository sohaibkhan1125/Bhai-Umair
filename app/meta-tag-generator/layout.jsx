export const metadata = {
    title: "SEO Meta Tag Generator - Optimize Your Website Head Tags",
    description: "Generate meta titles, descriptions, and keywords for your web pages. Boost your search engine rankings with perfectly formatted meta tags.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
