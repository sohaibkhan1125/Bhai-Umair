export const metadata = {
    title: "Image to Base64 Converter - Embed Images Directly in Code",
    description: "Convert JPEG, PNG, or GIF images to Base64 strings. Effortlessly embed images into HTML, CSS, or JSON files for faster web delivery.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
