export const metadata = {
    title: "Bootstrap Grid Generator - Build Responsive Layouts Fast",
    description: "Design custom Bootstrap grid layouts visually. Generate HTML and CSS code for responsive website structures in seconds.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
