export const metadata = {
    title: "Domain Age Checker - Find Out When a Domain Was Registered",
    description: "Check the exact age of any domain. Find registration dates and historical information with our free online domain age tool.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
