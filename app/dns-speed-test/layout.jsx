export const metadata = {
    title: "DNS Speed Test - Optimize Your Domain Name Resolution",
    description: "Test and compare DNS resolution speeds from different locations. Ensure your website loads faster by choosing the best DNS provider.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
