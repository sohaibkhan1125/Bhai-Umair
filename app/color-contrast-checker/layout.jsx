export const metadata = {
    title: "Color Contrast Checker - WCAG Accessibility Compliance",
    description: "Check if your color combinations meet WCAG contrast ratio requirements. Ensure your website is accessible to users with visual impairments.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
