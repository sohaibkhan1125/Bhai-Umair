export const metadata = {
    title: "CSS Box Shadow Generator - Create Custom Shadow Effects",
    description: "Design and generate CSS box shadow effects visually. Adjust blur, spread, color, and inset to get the perfect shadow for your web elements.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
