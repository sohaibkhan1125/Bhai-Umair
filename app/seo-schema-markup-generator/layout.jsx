export const metadata = {
    title: "SEO Schema Markup Generator - Boost Your Search Visibility",
    description: "Generate JSON-LD schema markup for your website. Improve your search results with rich snippets for articles, events, products, and more.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
