export const metadata = {
    title: "JavaScript Minifier - Optimize Script Performance",
    description: "Minify your JS code to reduce file size and boost page speed. Strip unnecessary characters from your JavaScript files for production.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
