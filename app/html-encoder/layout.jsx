export const metadata = {
    title: "HTML Entity Encoder & Decoder - Secure Your Web Content",
    description: "Convert special characters into HTML entities and vice versa. Protect your code from XSS and ensure proper browser rendering.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
