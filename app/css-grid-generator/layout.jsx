export const metadata = {
    title: "CSS Grid Generator - Visual Layout Builder for Web Design",
    description: "Build complex CSS Grid layouts visually. Generate HTML and CSS code for responsive website structures with our easy-to-use grid tool.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
