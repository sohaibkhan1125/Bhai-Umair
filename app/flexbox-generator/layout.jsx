export const metadata = {
    title: "CSS Flexbox Generator - Visual Layout Tool",
    description: "Design responsive Flexbox layouts visually. Adjust alignment, direction, and spacing and get the CSS code instantly.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
