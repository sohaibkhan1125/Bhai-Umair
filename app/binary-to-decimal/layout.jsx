export const metadata = {
    title: "Binary to Decimal Converter - Precise Number Conversion",
    description: "Convert binary numbers to decimal and decimal to binary with our free online converter. Simple, fast, and accurate number system conversion.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
