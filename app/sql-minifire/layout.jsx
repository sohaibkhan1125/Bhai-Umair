export const metadata = {
    title: "SQL Minifier - Compress Your Database Queries",
    description: "Reduce the size of your SQL scripts by removing unnecessary whitespace and comments. Optimize query transmission and storage.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
