export const metadata = {
    title: "JSON Formatter & Validator - Beautify Your Data",
    description: "Format, beautify, and validate JSON code. Make complex data structures easy to read and identify syntax errors instantly.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
