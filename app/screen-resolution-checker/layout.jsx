export const metadata = {
    title: "Screen Resolution Checker - Test Your Display Settings",
    description: "Quickly detect your screen's resolution, aspect ratio, and pixel density. Essential tool for web designers and developers to test responsiveness.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
