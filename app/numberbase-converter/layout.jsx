export const metadata = {
    title: "Number Base Converter - Binary, Octal, Hex, Decimal",
    description: "Convert numbers between different bases like binary, octal, decimal, and hexadecimal. Fast and accurate number system conversion tool.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
