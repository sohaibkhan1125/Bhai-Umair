export const metadata = {
    title: "Online DNS Lookup Tool - Check DNS Records Instantly",
    description: "Perform comprehensive DNS lookups for any domain. View A, MX, CNAME, TXT, and NS records to troubleshoot domain issues.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
