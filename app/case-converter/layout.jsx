export const metadata = {
    title: "Online Case Converter - Uppercase, Lowercase, Title Case",
    description: "Easily convert text between different cases: uppercase, lowercase, sentence case, title case, and camelCase. Fast and free text formatting tool.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
