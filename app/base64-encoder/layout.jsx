export const metadata = {
    title: "Base64 Encoder & Decoder - Securely Encode Your Data",
    description: "Fast and secure online Base64 encoder and decoder. Convert text or images to Base64 format and vice versa instantly.",
};

export default function Layout({ children }) {
    return <>{children}</>;
}
